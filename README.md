# Quadratic-Equation-Solver


A Python app with a Tkinter GUI that solves quadratic equations of the form ax² + bx + c = 0. 
It calculates and displays the **number of solutions** as well as the **roots** (real or complex).

## Features
- Input fields for coefficients a, b, and c
- Calculates and displays real and complex roots
- Shows the number of solutions
- Handles invalid inputs gracefully

## How to Run
1. Requires **Python 3.x** and Tkinter (usually included with Python)
2. Run the program:
```bash
python quadratic_solver.py
.
